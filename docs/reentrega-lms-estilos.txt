Profe Sergio, gracias por la revisión — y disculpe, el error fue mío en la
entrega.

Le pasé el repositorio sin avisarle que la práctica vive en la rama
`practicas-ebac`, no en `main`. Al abrir el repo se ve `main` por defecto, y ahí
la práctica literalmente no está: `main` ni siquiera tiene styled-components en
el package.json. Por eso su lista de faltantes era exactamente la lista de todo
lo que entregué. Ya cambié la rama por defecto del repositorio para que no vuelva
a pasar.

Aparte de eso, dos de sus puntos SÍ eran trabajo pendiente y ya están hechos.


DÓNDE MIRAR

  Rama:   practicas-ebac
  Commit: c37e717e

  git clone -b practicas-ebac https://github.com/pipeTawns-x/Landingpages-Carni.pwa.git


PUNTO POR PUNTO

createGlobalStyle
  src/styles/globalStyles.ts:18

Tema compartido y ThemeProvider
  src/theme/carniTheme.ts:9      el tema
  src/entry/products.tsx:458     el ThemeProvider
  src/theme/styled.d.ts:12       los tipos, para que el tema autocomplete

Componentes creados con styled-components
  Seis: CartPanel, CategoryCard, Lupa, OrderList, ProductCard y ProductList.
  CategoryCard era el que faltaba y ya está migrado.

Estilos dinámicos con props
  CartPanel/styles.ts     $isOpen        mueve el cajón del pedido
  ProductCard/styles.ts   $size          el tamaño de la tarjeta
  Lupa/styles.ts          $open          levanta el buscador

Un archivo de estilos por componente
  Este era el punto donde más razón tenía. No había ninguno: los 28 componentes
  estilizados vivían dentro de los .tsx. Lupa.tsx tenía 23 adentro, junto a toda
  su lógica.

  Ahora hay seis archivos, uno por componente:

    src/components/CartPanel/styles.ts
    src/components/CategoryCard/styles.ts
    src/components/Lupa/styles.ts
    src/components/OrderList/styles.ts
    src/components/ProductCard/styles.ts
    src/components/ProductList/styles.ts

  Y ya no queda ningún .css ni .scss dentro de src/components.


UNA ACLARACIÓN SOBRE EL SCSS QUE VIO

Los archivos SCSS que quedan en el proyecto están en `css/`, y son el sistema
7-1 del sitio: la rejilla del bento, el encabezado, el modo oscuro. Eso es la
parte que no es React y no formaba parte de esta práctica.

De hecho, al migrar CategoryCard tuve que decidir qué NO mover. Sus reglas de
posición viven en `css/pages/_bento-main.scss`, así que el componente conserva
sus clases y styled-components agrega la suya además, no en lugar de. Si me
llevaba esas reglas al componente, el tile se salía de su celda del bento.

Verifiqué que nada cambió visualmente: la tarjeta lleva su clase de
styled-components y sigue conservando su grid-area.

Un salUdos.
