# Contexto de la sesión — 21 de agosto 2026

Continúa `docs/CONTEXTO_2026-08-20.md`. Todo verificado contra el repo.

---

## Estado

```
Rama       practicas-ebac
HEAD       ba3e025f  "fix(db): precio server-side, minimos de compra y agentes locales"
Sincronía  1 adelante de origin — SIN pushear
PR         #5 abierto a main, sin mergear
Grafo      graphify-out actualizado hoy 11:32
Hooks      pre-commit (GGA) · post-commit (graphify) · post-checkout (graphify)
```

## Lo que se hizo hoy

**Commit `ba3e025f`** — 12 archivos, 908 inserciones. GGA pasó en verde sin
`--no-verify`: su filtro es `*.ts,*.tsx,*.js,*.jsx,*.py,*.go` y el commit no
toca ninguno. Entraron:

- `supabase/migrations/202608210001_precio_server_side.sql`
- `docs/PENDIENTES.md`, `docs/CONTEXTO_2026-08-20.md`
- `.claude/agents/` — los 9 agentes

**Grafo de graphify actualizado.** Llevaba 27 días de atraso.

```
archivos    116 → 166      (los 50 nuevos son todo agosto)
nodos       831 → 1243
aristas     966 → 1373
comunidades 137
extracción  100% EXTRACTED · 0% INFERRED · 0 tokens de costo
```

Hallazgos del grafo:

- **God node frágil**: `showNotification()` en `js/modules/app.js` tiene 13
  aristas y la llaman desde 4 lugares de `js/modules/pages/dashboard.js`. Si
  se rompe, caen cuatro flujos del panel admin a la vez.
- Otros god nodes: `compilerOptions` (27), `OfflineManager` (19), `Product` (15)
- **Cero ciclos de import.** La estructura está sana.

**Hooks instalados**: `graphify hook install` puso `post-commit` y
`post-checkout`. El grafo ahora se reconstruye solo en cada commit. El
`pre-commit` de GGA quedó intacto — son tres archivos distintos.

## Lo que se descubrió y duele

**gentle-ai NO guarda contexto entre sesiones.** Verificado: lo único que
produce es `.atl/skill-registry.md`, y su propio contrato dice *"This registry
is an index, not a summary. Delegator use only."* Es un directorio de dónde
vive cada skill. No guarda decisiones, no guarda historial. Eduardo creía que
sí lo hacía. Nunca lo hizo.

Además ese registry está fechado el 17 de agosto: no conoce los 9 agentes ni
la skill `pendientes-carni`.

**Balance honesto de contexto agéntico:**

| Herramienta | Qué hace | ¿Guarda contexto entre sesiones? |
|---|---|---|
| gentle-ai | índice de skills | No |
| graphify | grafo de estructura de código | Del código sí, de decisiones no |
| engram | sin rastro en el repo | No verificado |
| `docs/CONTEXTO_*.md` | escrito a mano | Sí, pero manual |
| `docs/PENDIENTES.md` | deuda, con su skill | Sí, pero manual |

Lo único que hoy conserva contexto real entre sesiones son dos markdown y el
grafo del código. El resto son índices o herramientas puntuales.

**El hueco de fondo**: el grafo se actualiza solo (hook), pero las
*decisiones* no. Por qué el MCP está en solo lectura, por qué
`publicDir: false` no es el arreglo, por qué el carrito va al final — nada de
eso sale del código, así que ningún grafo lo va a ver.

## Pistas de investigación abiertas

Video de Gentleman Programming, título verificado:
**"La EVOLUCIÓN del contexto compartido entre AGENTES: Engram Cloud"**
(`youtube.com/watch?v=JPZkbGgJNUQ`). Sugiere que Engram tiene un producto de
nube para contexto compartido entre agentes. Sin verificar qué hace ni si
está configurado aquí.

Otros sin ver: `youtu.be/UoS_LP-PCG8`, `youtu.be/6ChZMEMJ8hA`.

**Limitación técnica encontrada**: el sandbox no alcanza YouTube (proxy 403),
así que `markitdown` no puede sacar transcripciones desde ahí. La vía que sí
funciona es el navegador con sesión iniciada.

**`--wiki` de graphify no existe en la versión instalada (0.8.46).** Es la
opción que generaría artículos markdown navegables para compartir contexto
entre OpenCode, Kimi y Claude Code. Actualizar es instalación global,
pendiente de autorización.

## Decisiones de Eduardo

- El commit `ba3e025f` **no se queda en `practicas-ebac`** — esa rama es la
  entrega EBAC y no debe cargar trabajo de backend.
- **No va a `main` todavía**: la migración arregla el hueco de seguridad pero
  aún no está aplicada. Mergear el `.sql` sin correrlo deja `main` diciendo
  una cosa y la base real otra.
- Orden acordado: aplicar la migración → arreglar `p_address` →
  rama propia → PR a main.

## Sigue bloqueado

- **Docker apagado.** Sin él no hay Supabase local, y el remoto pide
  `SUPABASE_DB_PASSWORD`. Por eso no se sabe si `202608210001` está aplicada.
- `js/modules/supabase.js:205` manda `p_address` pero la función espera
  `p_delivery_address`. La llamada RPC falla siempre. Sin arreglar.
